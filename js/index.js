$(document).ready(function () {

	console.log($(".sttitle"))
	var pathArray = $(".sttitle")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	

$(document).ready(function () {

	console.log($(".line1-1"))
	var pathArray = $(".line1-1")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	

$(document).ready(function () {

	console.log($(".line5-1"))
	var pathArray = $(".line5-1")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	


$(document).ready(function () {

	console.log($(".st1"))
	var pathArray = $(".st1")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	


$(document).ready(function () {

	console.log($(".stline2"))
	var pathArray = $(".stline2")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	

$(document).ready(function () {

	console.log($(".bgline1-1"))
	var pathArray = $(".bgline1-1")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	


$(document).ready(function () {

	console.log($(".st2"))
	var pathArray = $(".st2")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	

$(document).ready(function () {

	console.log($(".st4"))
	var pathArray = $(".st4")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	

$(document).ready(function () {

	console.log($(".bgline3-2"))
	var pathArray = $(".bgline3-2")

	$.each(pathArray, function (i, d) {
		

		var pathLength = d.getTotalLength()
		console.log(pathLength)

		$(d).css("stroke-dasharray", pathLength + " " + pathLength)
		$(d).css("stroke-dashoffset", pathLength)
	})	
})	